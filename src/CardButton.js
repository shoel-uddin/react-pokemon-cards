import React from 'react'

function CardButton(props) {
    return (
        <button
            onClick={props.GetCard}>
            Card Button!
        </button>
    )
}



export default CardButton;